# Environment Variables Template
# Copy this to .env.local and fill in your API keys
# NEVER commit .env.local to GitHub!

# AI Provider Configuration
# Priority: Gemini (primary) > DeepSeek (secondary) > Mock (fallback)

# Gemini API Key (Primary AI Provider)
# Get from: https://aistudio.google.com/apikey
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# DeepSeek API Key (Secondary AI Provider)
# Get from: https://platform.deepseek.com/api_keys
VITE_DEEPSEEK_API_KEY=your_deepseek_api_key_here

# Note: At least one API key is required for AI features
# Without any API key, the app runs in demo mode with mock responses
